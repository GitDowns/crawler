import loginimport downloadimport soupimport requestsimport getcourseimport Dataif __name__ == '__main__':    print('start')    r = ''    form = {}    session = requests.Session()    uid = 'u1510022'    pwd = '19950513'    r = login.login(uid, pwd, session, form)    form = Data.getsutdentinfo(r)    text, form = Data.getcourses(session, form)    formset = Data.getcousesinfo(text)    Data.archive(formset, form, session, text)